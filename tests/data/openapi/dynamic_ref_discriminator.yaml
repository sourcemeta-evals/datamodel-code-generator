openapi: "3.1.0"
info:
  title: Test
  version: "1.0"
paths: {}
components:
  schemas:
    ComparisonFilter:
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
          enum:
            - eq
            - ne
        key:
          type: string
        value:
          type: string
      required:
        - type
        - key
        - value
    CompoundFilter:
      $dynamicAnchor: CompoundFilter
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
          enum:
            - and
            - or
        filters:
          type: array
          items:
            oneOf:
              - $ref: "#/components/schemas/ComparisonFilter"
              - $dynamicRef: "#CompoundFilter"
            discriminator:
              propertyName: type
      required:
        - type
        - filters
