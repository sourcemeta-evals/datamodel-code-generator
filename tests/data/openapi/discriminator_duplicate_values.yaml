openapi: "3.1.0"
info:
  title: Test
  version: "1.0"
paths: {}
components:
  schemas:
    Result:
      oneOf:
        - $ref: "#/components/schemas/FirstVariant"
        - $ref: "#/components/schemas/SecondVariant"
      discriminator:
        propertyName: kind
    FirstVariant:
      $dynamicAnchor: FirstVariant
      type: object
      properties:
        kind:
          type: string
          enum:
            - shared
        first_field:
          type: string
        nested:
          type: array
          items:
            $dynamicRef: "#FirstVariant"
      required:
        - kind
    SecondVariant:
      type: object
      properties:
        kind:
          type: string
          enum:
            - shared
        second_field:
          type: string
      required:
        - kind
